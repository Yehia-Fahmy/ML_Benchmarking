# Multi-Model Image Generation Benchmark Requirements
# Supports Z-Image-Turbo, SD-Turbo, SD 1.5, and STARFlow

# Core Dependencies
# Install diffusers from source for Z-Image support
git+https://github.com/huggingface/diffusers

# PyTorch and Vision
torch>=2.0.0
torchvision>=0.15.0

# Model Dependencies
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0

# Hugging Face Hub (for model downloads)
huggingface_hub>=0.16.0

# Configuration and Data
pyyaml>=6.0

# Performance Monitoring
psutil>=5.9.0

# Optional: NVIDIA GPU monitoring (will be imported conditionally)
# Uncomment if you have NVIDIA GPU and want detailed GPU metrics
# gputil>=1.4.0

# Image Processing
Pillow>=9.0.0

# Note: For STARFlow support, additional dependencies may be required
# See STARFLOW_README.md for detailed setup instructions
